import { AppdragDirective } from './appdrag.directive';

describe('AppdragDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new AppdragDirective();
    expect(directive).toBeTruthy();
  });
});
